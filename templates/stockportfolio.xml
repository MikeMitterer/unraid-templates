<?xml version="1.0"?>
<Container version="2">
  <Name>stockportfolio</Name>
  <Repository>mangolila/stockportfolio:latest</Repository>
  <Registry>https://hub.docker.com/r/mangolila/stockportfolio</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/MikeMitterer/stockportfolio/issues</Support>
  <Project>https://github.com/MikeMitterer/stockportfolio</Project>
  <Overview>Portfolio management with market data, valuation, charts and tolerance-band rebalancing in your browser. Quotes, asset types and price history come from a separate StockInfo API. Trade plans are simulations; no orders are placed.[br][br]Portfolio data and settings are stored only in the browser, not on Unraid. No appdata volume is needed. Export backups under Settings - Backup; server backups do not include browser data. Keep the web address and host port stable when updating.[br][br]Set STOCKINFO_API_URL to an address reachable from your browser. StockInfo must allow the web app origin through CORS. HTTPS pages require an HTTPS API.[br][br]The container serves the web app on port 8080 without root privileges. Older images used port 80: update existing port mappings when upgrading.</Overview>
  <Category>Tools: Productivity:</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/MikeMitterer/unraid-templates/master/templates/stockportfolio.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/MikeMitterer/stockportfolio/master/public/stockportfolio-icon.png</Icon>
  <Screenshot>https://raw.githubusercontent.com/MikeMitterer/stockportfolio/master/docs/images/dashboard.png</Screenshot>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>A StockInfo API reachable from the browser.</Requires>
  <Config Name="WebUI Port" Target="8080" Default="8088" Mode="tcp" Description="Host port for the web app. The container listens on 8080. Keep the host address and port stable to retain the browser storage origin." Type="Port" Display="always" Required="true" Mask="false">8088</Config>
  <Config Name="StockInfo API" Target="STOCKINFO_API_URL" Default="" Mode="" Description="URL of your StockInfo API, reachable from the browser (for example https://stockinfo.example.com). Required; no built-in public API. Restart the container after changing it." Type="Variable" Display="always" Required="true" Mask="false"/>
  <Config Name="Timezone" Target="TZ" Default="UTC" Mode="" Description="Timezone for container logs, for example Europe/Vienna. The web interface follows the browser timezone." Type="Variable" Display="always" Required="false" Mask="false">UTC</Config>
</Container>
